---
interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className = '', ...rest } = Astro.props;

const classes = [
  'flex flex-col space-y-1.5 p-6',
  className,
].join(' ');
---

<div class={classes} {...rest}>
  <slot />
</div>