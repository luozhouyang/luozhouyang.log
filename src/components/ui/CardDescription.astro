---
interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className = '', ...rest } = Astro.props;

const classes = [
  'text-sm text-muted-foreground',
  className,
].join(' ');
---

<p class={classes} {...rest}>
  <slot />
</p>